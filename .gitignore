# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.py_compile_cache/
venv/
.venv/
.env
.env.*

# Spring / Gradle
server-spring/.gradle/
server-spring/build/
build/

# PyInstaller (client_deployment.md) — 루트의 PyInstaller 출력만 무시.
# (grafana-plugins/<id>/dist 는 의도적으로 커밋 → NCP 가 npm 없이 git pull 로 패널 사용)
/dist/
*.spec

# Local agent/tool state
.claude/
.codex/

# Root-level project documents
/*.docx
/*.md
!/README.md
!/CONTRIBUTING.md

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# LocalQueue persistence files
.agent_queue.jsonl
.agent_queue.jsonl.tmp

# Temporary debug helpers + eval result snapshots under tools/.
tools/_dbg_*.py
tools/_*.json
tools/_*.csv

# Provisioning toolkit outputs — contain raw API keys, never commit.
tools/provisioned_keys*.csv
tools/*.key
provisioned_keys*.csv
# sim_fleet 로컬 발급 키/대상 (raw 키 포함 — 절대 커밋 금지)
sim_keys*.csv
sim_pcs*.csv

# pgvector 로컬 전용 (커밋 안 함)
# - override.yml: 개인용 auto-load pgvector overlay (공용은 docker-compose.pgvector.yml)
# - .poc/: PoC/벤치 검증 스크립트(throwaway)
docker-compose.override.yml
.poc/
